/**
 * Shared UI package for Verevia apps.
 *
 * Technical skeleton only — no component library installed yet and no
 * domain components. shadcn/ui setup (Tailwind, cn() helper, components.json)
 * lives in apps/web, since shadcn generates component source directly into
 * the consuming app rather than a pre-built npm package.
 */

export const UI_PACKAGE_NAME = "@verevia/ui";
